#pragma once

namespace globals {
	const int SCREEN_WIDTH = 900;
	const int SCREEN_HEIGHT = 700;
}

namespace filename {
	const char scoreFile[] = "userinfo.dat";
};

namespace winners {
	enum side {
		NONE,
		LEFT,
		RIGHT
	};
};